你是 AI 与自动驾驶领域的顶级研究专家，请对以下候选论文**逐篇**进行精细评分。

## 关注的研究方向
${topics_of_interest}

## 评分维度（各 0-10 分）
- **relevance_score**：与上述方向的相关程度
- **novelty_score**：方法新颖性与贡献潜力
- **total_score**：上述两个维度之和（0-${raw_score_max}），作为原始排序依据（后续代码会叠加作者/顶会奖励和黑名单降权，请不要自行调整分数倾向）

## 候选论文
${paper_blocks}

## 输出要求
只返回 JSON，顶层字段 `ranked_papers` 是一个数组，**必须覆盖全部 ${paper_count} 篇候选**（哪怕分数很低也要出现，不要遗漏）。
每项字段：rank（按 total_score 从高到低排名）、arxiv_id、relevance_score（0-10）、novelty_score（0-10）、total_score（relevance + novelty，0-${raw_score_max}）、topic_category、one_line_summary（中文一句话总结）。
不要输出任何解释文字，不要截断列表。
